export interface Patient {
    id: string;
    full_name: string;
    last_sesion: string;
    status: 'active' | 'completed' | 'pending';
}

export interface BehavioralTasks {

}

export interface Sessions {
    
}